# Supabase
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here

# NOTE: Do NOT add VITE_WEBHOOK_SECRET here.
# Edge Functions are authenticated via Supabase JWT (Authorization: Bearer <token>).
# Secrets belong in Supabase Edge Function environment variables, never in the client bundle.
